first_number = float(input("enter the dividend: "))
second_number = float(input("enter the divisor: "))

remainder = first_number % second_number

print("the remainder of", float(first_number),
      "/", float(second_number), "=", remainder)
