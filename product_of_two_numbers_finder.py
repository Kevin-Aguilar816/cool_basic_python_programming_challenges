number_one = float(input("enter a multiplicand: "))
number_two = float(input("enter a multiplier: "))

product = number_one * number_two

print("the product of", float(number_one), "and",
      float(number_two), "is equal to", float(product))
